Addition=input().split() #While it gets a space, it would split that part in two
a,b=Addition #The splitted part would be divided into a and b
summation=int(a)+int(b) #If I won't use format specifier, then it would just add them as string addition
print(summation)